# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/chatlink
# For production, use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chatlink

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-secure

# Client Configuration
CLIENT_URL=http://localhost:3000
# For production, use your deployed frontend URL:
# CLIENT_URL=https://yourusername.github.io/chatlink-frontend

# File Upload Configuration
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpeg,jpg,png,gif,pdf,docx,doc,txt,mp4,avi,mov,mp3,wav

# Security Configuration
BCRYPT_ROUNDS=12
TOKEN_EXPIRY=24h
GUEST_TOKEN_EXPIRY=2h

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100